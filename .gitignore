__pycache__/
*.py[cod]
.venv/
venv/

# Datasets and tokenized binaries — regenerate with train_tokenizer.py
data/raw/*
data/processed/*
!data/**/.gitkeep

# Checkpoints are large; the model ships via the Hugging Face Space, not here
train/checkpoints/*.pt
!train/checkpoints/.gitkeep

# Assembled Space upload (duplicate of the code above + the 55MB model)
space/

.ipynb_checkpoints/
.DS_Store
